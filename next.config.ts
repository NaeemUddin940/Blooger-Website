import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "blogger.googleusercontent.com", "example.com", "images.remotePatterns", "img.drz.lazcdn.com"],
  },
};

export default nextConfig;
